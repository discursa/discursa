import React, { FC } from "react"
import styles from "./PreviewableMessage.module.scss"

// TODO: Write PreviewableMessage component UI and API

export const PreviewableMessage: FC<PreviewableMessageProps> = () => (
  <div className={styles.PreviewableMessage}>PreviewableMessage Component</div>
)
